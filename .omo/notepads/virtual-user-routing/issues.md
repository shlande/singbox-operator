# Issues

## [2026-05-01] Session Start

### Pre-existing issues (not blocking our work)
- Several files use wrong module path `github.com/your-org/singbox-operator` instead of `github.com/shlande/singbox-operator`
- Helm chart YAML templates have LSP errors (Helm template syntax not valid YAML)
- These are pre-existing and NOT introduced by our changes
