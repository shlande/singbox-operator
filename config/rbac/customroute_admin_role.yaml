apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: sing-box-operator-2
    app.kubernetes.io/managed-by: kustomize
  name: customroute-admin-role
rules:
- apiGroups:
  - singboxoperator.shlande.top
  resources:
  - customroutes
  verbs:
  - '*'
- apiGroups:
  - singboxoperator.shlande.top
  resources:
  - customroutes/status
  verbs:
  - get
