apiVersion: singboxoperator.shlande.top/v1alpha1
kind: SingBoxNode
metadata:
  labels:
    app.kubernetes.io/name: sing-box-operator-2
    app.kubernetes.io/managed-by: kustomize
  name: singboxnode-sample
spec:
  # TODO(user): Add fields here

  # # Example: restrict which inbound nodes may use this outbound node.
  # # Empty or omitted = allow all (backward compatible).
  # # When set, only the listed inbound node names may use this node as outbound.
  # allowedInbounds:
  #   - "my-inbound-node"

  # # Example: restrict which outbound nodes this inbound node may use.
  # # Empty or omitted = allow all same-region outbounds (backward compatible).
  # # When set, ALL outbound paths (same-region auto-discovery AND CustomRoute)
  # # are gated by this whitelist.
  # allowedOutbounds:
  #   - "my-outbound-node"
