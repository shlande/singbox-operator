Thank you for installing {{ .Chart.Name }} v{{ .Chart.Version }}!

The sing-box operator is now running in namespace {{ .Release.Namespace }}.

To get started, create your first SingBoxNode:

  kubectl apply -f https://raw.githubusercontent.com/shlande/singbox-operator/main/charts/singbox-operator/examples/singboxnode-inbound.yaml

For more examples, see the examples/ directory in the chart.

Operator status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ .Chart.Name }}

CRDs installed:
  kubectl get crd | grep shlande.top
