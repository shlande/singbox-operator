{{- if and .Values.webhook.enabled .Values.certManager.enabled .Values.certManager.createIssuer }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "singbox-operator.fullname" . }}-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "singbox-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
